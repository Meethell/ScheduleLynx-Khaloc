<Window
    x:Class="Client.Views.CustomMessageBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="clr-namespace:Client.Domain"
    xmlns:local="clr-namespace:Client.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="MessageBoxWindow"
    AllowsTransparency="True"
    Background="Transparent"
    BorderThickness="1"
    FontFamily="{DynamicResource MaterialDesignFont}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    TextElement.FontSize="10"
    TextElement.FontWeight="Regular"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    WindowStyle="None"
    mc:Ignorable="d">
    <!--  Đảm bảo Background không phải là Transparent  -->
    <Window.Resources>
        <domain:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <domain:BoolToColorConverter x:Key="BoolToColorConverter" />
        <Style TargetType="{x:Type Window}">
            <Setter Property="shell:WindowChrome.WindowChrome">
                <Setter.Value>
                    <shell:WindowChrome
                        CaptionHeight="0"
                        CornerRadius="10"
                        GlassFrameThickness="0"
                        UseAeroCaptionButtons="False" />
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <GroupBox
            Width="auto"
            Height="auto"
            Padding="0"
            Style="{StaticResource MaterialDesignCardGroupBox}">
            <GroupBox.Header>
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="White"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="{Binding Message}" />
            </GroupBox.Header>
            <Grid>
                <Grid>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <materialDesign:TransitioningContent Margin="10,5">
                                <materialDesign:TransitioningContent.OpeningEffects>
                                    <materialDesign:TransitionEffect Kind="FadeIn" />
                                    <materialDesign:TransitionEffect Kind="SlideInFromBottom" />
                                </materialDesign:TransitioningContent.OpeningEffects>
                                <TextBlock
                                    x:Name="TxtMessage"
                                    Grid.Row="0"
                                    MaxWidth="400"
                                    MaxHeight="200"
                                    Margin="5"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    materialDesign:HintAssist.Hint=""
                                    Style="{StaticResource MaterialDesignBody2TextBlock}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </materialDesign:TransitioningContent>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button
                                x:Name="btnOk"
                                Grid.Column="1"
                                Width="90"
                                Margin="5"
                                Content="OK"
                                Style="{StaticResource MaterialDesignRaisedDarkButton}"
                                ToolTip="Ok"
                                Visibility="{Binding IsInformation, Converter={StaticResource BoolToVis}}" />
                            <!--<Button x:Name="btnCancel" Grid.Column="2" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5" Width="100" Content="Cancel" ToolTip="Cancel" Foreground="White" Visibility="{Binding IsConfirmation, Converter={StaticResource BooleanToVisibilityConverter}}"/>-->
                            <Button
                                x:Name="btnYes"
                                Grid.Column="1"
                                Width="90"
                                Margin="5"
                                Content="Xác nhận"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                ToolTip="Xác nhận"
                                Visibility="{Binding IsConfirmation, Converter={StaticResource BoolToVis}}" />
                            <Button
                                x:Name="btnNo"
                                Grid.Column="2"
                                Width="80"
                                Margin="5"
                                Content="Hủy"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                ToolTip="Hủy"
                                Visibility="{Binding IsConfirmation, Converter={StaticResource BoolToVis}}" />
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </GroupBox>


    </Grid>
</Window>

